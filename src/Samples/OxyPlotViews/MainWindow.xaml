<Window
    x:Class="Elmish.WPF.Samples.OxyPlot.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:oxy="http://oxyplot.org/wpf"
    Title="Single Plot" Height="620" Width="500"
    WindowStartupLocation="CenterScreen">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition />
      <RowDefinition />
    </Grid.RowDefinitions>
    <Button Content="{Binding ToggleStartStopLabel}" Command="{Binding ToggleStartStop}" Height="20" Margin="10" />
    <oxy:PlotView Grid.Row="1" Model="{Binding LinePlotModel}" />
    <oxy:PlotView Grid.Row="2" Model="{Binding BoxPlotModel}" />
  </Grid>
</Window>
