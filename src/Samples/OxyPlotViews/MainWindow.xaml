<Window
    x:Class="Elmish.WPF.Samples.OxyPlot.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:oxy="http://oxyplot.org/wpf"
    Title="Single Plot" Height="120" Width="500"
    WindowStartupLocation="CenterScreen">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition />
    </Grid.RowDefinitions>
    <Button Content="{Binding ToggleStartStopLabel}" Command="{Binding ToggleStartStop}" Height="20" Margin="10" />
    <oxy:PlotView Grid.Row="1" Model="{Binding PlotViewModel}" />
  </Grid>
</Window>
